import { StyleSheet, Dimensions } from 'react-native';

import { theme } from '../styles';
import { sizeScreen } from '../../helpers';

const styles = StyleSheet.create({
  header: {
    paddingHorizontal: 0,
  },
  content: {
    flexGrow: 1,
  }
});

export default styles;
