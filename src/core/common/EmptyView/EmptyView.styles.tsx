import {StyleSheet} from 'react-native';

import {theme} from '../styles';
import {sizeHeight} from '@core/helpers/sizeScreen'

const styles = StyleSheet.create({
  emptyBlock: {
    height: sizeHeight(100),
  },
});

export default styles;
